const PackageMostSoldByUser = ({packageName,timesSold}) => {
    return (
        <div>
            <p>Package: {packageName}</p>
            <p>Times Sold: {timesSold}</p>
        </div>
    )
}

export default PackageMostSoldByUser
